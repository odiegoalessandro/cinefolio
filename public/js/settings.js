async function setupSettings() { try { const {user}=await Api.get('/api/auth/me'); for (const [key,value] of Object.entries(user)) { const input=document.querySelector(`[name="${key}"]`); if(input) input.value=value || ''; } } catch { location.href='/login.html'; } }
document.querySelector('#settings-form')?.addEventListener('submit', async e => { e.preventDefault(); try { await Api.put('/api/profile', Object.fromEntries(new FormData(e.target))); document.querySelector('#settings-message').textContent='Perfil atualizado.'; } catch(error) { document.querySelector('#settings-message').textContent=error.message; }});
document.querySelector('#delete-account')?.addEventListener('click', async () => { if (confirm('Excluir sua conta e todos os dados?')) { await Api.delete('/api/account'); location.href='/index.html'; }}); setupSettings();
